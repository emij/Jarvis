#JSGF V1.0;

/**
 * JSGF Grammar for Voice controlled home, Jarvis
 *
 * @author Marika Hansson
 * @author Markus Otterberg
 */

grammar jarvis;

<onSwitch> = ([Turn] On | Enable | Activate) {appObj.getCommandObject().addAction("on");};
<offSwitch> = ([Turn] Off | Disable | Deactivate) {appObj.getCommandObject().addAction("off");};
<device> = (Lamp | Lights | lamps) {appObj.getCommandObject().addDevice("lamp");};
<room> = Kitchen | Bedroom;

/* Rules for commands*/

public <light> = (<onSwitch> | <offSwitch>) [The] [<room>] <device> {appObj.getCommandObject().generateCommand();};